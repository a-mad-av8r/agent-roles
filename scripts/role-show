#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

role="${1:-}"
if [[ -z "$role" || ! -f "roles/$role.md" ]]; then
  echo "Usage: role-show <role>" >&2
  exit 1
fi

sed -n '1,220p' "roles/$role.md"
